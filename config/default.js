const config = {
  // 启动端口
  port: 80,
  // 数据库配置
  database: {
    DATABASE: 'nodesql',
    USERNAME: 'root',
    PASSWORD: 'm3g1997',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config